{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'FILE=\"$CLAUDE_TOOL_ARG_FILE_PATH\"; if echo \"$FILE\" | grep -qE \"docs/specs/.*\\.md$|backend/app/(api|services|agents|adapters|models|schemas)/.*\\.py$\"; then echo \"[SPEC-TEST-SYNC] Spec 또는 코드 변경 감지: $FILE — /gen-test로 테스트 재생성을 권장합니다. TEST_STRATEGY.md §4 참조.\"; fi'"
          }
        ]
      }
    ]
  }
}
